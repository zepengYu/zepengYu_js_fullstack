<%- include('header') %>
    <!-- html 模板 模板语法 ejs 套模板 <%%> -->
    <h1><%= name.toUpperCase() %></h1>
    <P>hello, <%= name %></P>
    <ul>
        <% for (var i = 0; i < supplies.length; i++ ) { %>
            <li><%= supplies[i] %></li>
        <% } %>
    </ul>
<%- include('footer') %>